name: a_watch
description: "Приложение для просмотра аниме"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  
  # HTTP
  http:
  
  # HTML парсинг
  html:
  
  # Кэширование
  hive:
  hive_flutter:
  path_provider:
  
  # BLoC
  flutter_bloc:
  equatable:
  
  # Другие
  url_launcher:
  intl:
  uuid:
  get_it: ^9.2.0
  
  # Theme and UI
  shared_preferences:

  # Image caching and optimization
  cached_network_image:

  # Video player for HLS streams

  # Window management for desktop
  window_manager:
  flutter_inappwebview: ^6.1.5
  
  # Video player - platform-specific libraries
  media_kit: ^1.2.6
  media_kit_video: ^2.0.1
  media_kit_libs_windows_video: ^1.0.11
  media_kit_libs_android_video: ^1.3.8
  media_kit_libs_ios_video: ^1.1.4
  media_kit_libs_linux: ^1.0.6
  
  # Provider for state management
  provider:

  # Color picker
  flutter_colorpicker: ^1.0.3

  # Navigation
  go_router: ^13.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:

  # Тестирование
  test:
  mockito:
  bloc_test: ^9.1.4
  build_runner: ^2.4.7

flutter:
  uses-material-design: true
